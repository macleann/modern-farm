//begin by exporting a function to create a wheat object
export const createWheat = () => {
    //define the wheat object with the appropriate properties
    const wheatObject = {
        type: "Wheat",
        height: 230,
        output: 6
    }
    //return the object
    return wheatObject
}